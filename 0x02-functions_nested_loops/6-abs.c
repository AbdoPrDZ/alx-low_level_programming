#include "main.h"
#include <stdio.h>

/**
 * print_sign - Prints the sign of a number.
 * @n: The number.
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
