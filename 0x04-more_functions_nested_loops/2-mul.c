#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: The first number.
 * @a: The second number.
 * Return: The multiplies of a and b.
 */
int mul(int a, int b)
{
	int m = a * b;

	return (m);
}
